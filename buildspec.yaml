version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto8
    commands:
        - apt update -y
  build:
    commands:
        - mvn clean package
        - mkdir output
        - cp target/*.war output/books.war
        - cp target/*executable.jar output/onlinebookstore-executable.jar
artifacts:
  files:
        - 'output/*.war'
        - 'output/*.jar'
        - appspec.yaml
        - scripts/*
